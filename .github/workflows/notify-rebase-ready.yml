name: Notify Rebase Ready

# 当 Copilot 的冲突解决 PR 通过 CI 后通知你
on:
  check_suite:
    types: [completed]

permissions:
  issues: write
  pull-requests: write

jobs:
  notify-on-copilot-pr-ready:
    if: >-
      github.event.check_suite.conclusion == 'success' &&
      startsWith(github.event.check_suite.head_branch, 'conflict/')
    runs-on: ubuntu-latest
    steps:
      - name: Find and comment on PR
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          BRANCH="${{ github.event.check_suite.head_branch }}"

          PR_NUMBER=$(gh pr list \
            --repo ${{ github.repository }} \
            --head "$BRANCH" \
            --json number \
            --jq '.[0].number' 2>/dev/null || echo "")

          if [[ -z "$PR_NUMBER" ]]; then
            echo "No PR found for branch $BRANCH"
            exit 0
          fi

          gh pr comment "$PR_NUMBER" \
            --repo ${{ github.repository }} \
            --body "## CI Passed - Rebase Ready
          
          Copilot has resolved conflicts and CI passed.
          
          **Next steps:**
          1. Review PR changes
          2. Merge and sync to target branch
          
          @${{ github.repository_owner }}"
